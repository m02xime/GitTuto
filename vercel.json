{
    "builds": [{
        "src": "MineGit/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "MineGit/wsgi.py"
        }
    ]
}